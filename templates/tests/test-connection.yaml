apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yaqoob-helm.fullname" . }}-test-connection"
  labels:
    {{- include "yaqoob-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yaqoob-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
