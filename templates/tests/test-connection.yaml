apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "assign.fullname" . }}-test-connection"
  labels:
    {{- include "assign.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "assign.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
