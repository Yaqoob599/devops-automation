apiVersion: v1
kind: Service
metadata:
  name: {{ include "assign.fullname" . }}
  labels:
    {{- include "assign.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "assign.selectorLabels" . | nindent 4 }}
